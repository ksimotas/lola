name: "${.optimizer}_${.learning_rate}_${.weight_decay}_${.scheduler}_${.grad_clip}"
optimizer: "shampoo"
betas: [0.9, 0.999, 0.999]
precondition_frequency: 16
percondition_dim: 4096
learning_rate: 1e-4
weight_decay: 0.0
warmup: 0
scheduler: "cosine"
grad_clip: 1

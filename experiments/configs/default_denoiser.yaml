defaults:
  - dataset: euler_all
  - denoiser: dit_large
  - denoiser/loss: beta
  - denoiser/schedule: log_logit
  - optim: soap

trajectory:
  length: 9
  stride: 4

train:
  epochs: 1024
  epoch_size: 16384
  batch_size: 64
  accumulation: 1
  ema_decay: 0.999

masking: true

ae_from: ???
fork_from: null
fork_target: "state"
